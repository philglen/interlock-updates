# Interlock 0.1.4 Private Alpha

- Adds **Forgot password?** to the sign-in screen.
- Adds **Interlock → Reset Local Password…** to the native app menu.
- Password reset remains local to the logged-in macOS user and revokes every existing Interlock session.
