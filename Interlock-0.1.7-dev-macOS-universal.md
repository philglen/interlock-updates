# Interlock 0.1.7 Private Alpha

Fixes persistent “Too many attempts” errors after a password reset when an older macOS background service remains active and points at a stale database.

After installing this update, fully quit and reopen Interlock, then use **Reset Local Password** once before signing in.
